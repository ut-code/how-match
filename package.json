{
  "name": "how-match",
  "type": "module",
  "workspaces": [
    "service",
    "web"
  ],
  "scripts": {
    "prepare": "trap 'kill 0' SIGINT; (cd service; bun run prepare) & (cd web; bun run prepare) & wait",
    "dev": "trap 'kill 0' EXIT; (cd service; bun run dev) & (cd web; bun run dev) & wait",
    "check": "(cd web && bun check) && (cd service && bun check)"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
